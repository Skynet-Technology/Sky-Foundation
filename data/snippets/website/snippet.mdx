```jsx
const whyUs = () = {
  // Your current missionary // community
  let struggle = true; 
  
  // What we can do to help you :D
  function reasons() {
  if (struggle = true) {
    return `We know what your going through and that's why we will build and expand your missionary // community`
  };

  else {
    return `OoO, that's great to hear you have no problems, but still looking for help? well - you got us :D`
  };
  };

  console.log(reasons())
}
```
